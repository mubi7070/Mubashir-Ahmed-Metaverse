/* 35. T-Shirt: Write a function called make_shirt() that accepts a size and the text of a message 
that should be printed on the shirt. The function should print a sentence summarizing the 
size of the shirt and the message printed on it. Call the function.
*/

function make_shirt(size, msg){
    console.log("Ohh! That will look great on T-Shirt of size: " + size + " and the message: " + msg);
}

make_shirt("Medium", "Don't Stop until you are done");